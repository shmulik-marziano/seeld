// DNA v3: the blueprint grid is retired — the pastel circles (.dna-circles)
// are the site's backdrop now (STYLESEED.md). The component stays exported
// so existing imports keep compiling; it renders nothing.

const GridBackdrop = () => null;

export default GridBackdrop;
