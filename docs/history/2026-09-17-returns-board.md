# לוח התשואות · דוח מסירה · 17.9.2026

ענף: `feature/returns-board`. מצב: תצוגה מקדימה, ממתין לאישור למיזוג ל-`main` (הדחיפה ל-`main` מפרסמת אוטומטית ל-seeld.co.il).

## מה נבנה

**שכבת נתונים (Supabase, פרויקט `lvifatyksqizwcutfbqp`)**
- `cma_board`: מטריאליזד־ויו מעל `cma_funds`. שורה לכל קופה בתקופת הדיווח האחרונה: חברה מקוצרת, מוצר (`board_product`, כולל זיהוי "חיסכון לכל ילד" משם הקופה), מסלול (`track`, קטגוריה מחושבת משם הקופה והסיווגים), תשואות חודש / מתחילת שנה / 3 / 6 / 12 חודשים (מצטברות מהתשואות החודשיות), 3 ו-5 שנים, דמי ניהול, סטיית תקן, שארפ, חשיפות, גיוסים נטו 12 חודשים, וסדרת 12 החודשים לגרף.
- `cma_company_map`: ויו רגיל מעל הלוח, נכסים וגיוסים לפי חברה ומוצר.
- פונקציות: `cma_board_product`, `cma_track_key`, `cma_company_short`.
- רענון: הקרון היומי `refresh-cma-funds-latest` (03:15 UTC) מרענן גם את הלוח; קרון נוסף `refresh-cma-board-monthly` ב-8 בחודש 04:30 אחרי הסנכרון החודשי.
- המיגרציה `20260917100000_cma_board.sql` הוחלה ורשומה בפרויקט (`20260917113722 cma_board`).

**API (Vercel edge, פרנקפורט)**
- `api/board.ts`, `api/company-map.ts`: קריאה עם מפתח anon, קאש `s-maxage=3600, stale-while-revalidate=86400`.

**אתר**
- `/return-tables`: מפת השוק לפי חברה (נכסים לפי מוצר, סך הכול, גיוסים 12 חודשים, 12 החברות הגדולות עם הרחבה) + שישה כרטיסי מוצר (מספר קופות, מסלולים, נכסים, המסלול המוביל ב-12 חודשים).
- `/return-tables/:product`: לוח לכל מוצר, מחולק למסלולים. לכל מסלול: כותרת עם נכסים ומספר קופות, תיאור, גרף (שש הגדולות כברירת מחדל), ייצוא אקסל, הדפסה, וטבלה ממוינת. חיפוש חופשי, סינון לפי חברה, תצוגה מלאה או מצומצמת (ברירת מחדל מצומצמת בטלפון).
- סלאגים: `pension`, `hishtalmut`, `gemel`, `gemel-invest`, `policies`, `child-savings`. סלאג לא מוכר מציג את עמוד הסקירה.
- SEO: כותרות ותיאורים לכל מוצר ב-`RouteSeo.tsx`. הפוטר מקשר ל"לוח התשואות".

## נתונים בזמן המסירה (תקופה 07/2026)

| מוצר | קופות | מסלולים | עם תשואת 12 חודשים | עם גיוסים |
|---|---|---|---|---|
| גמל | 308 | 17 | 287 | 308 |
| פנסיה | 291 | 16 | 279 | 291 |
| השתלמות | 234 | 13 | 228 | 234 |
| פוליסות חיסכון | 200 | 19 | 196 | 0 |
| גמל להשקעה | 152 | 12 | 144 | 152 |
| חיסכון לכל ילד | 41 | 5 | 37 | 41 |

לפוליסות חיסכון (ביטוחנט) אין נתוני הפקדות במקור, ולכן אין גיוסים. הלוח מסמן זאת בכוכבית.

## מה נבדק

- בנייה: עוברת. vitest 12/12. tsc: רק 11 השגיאות הקיימות באזור האישי.
- צילומי מסך ב-1440 וב-390 של הסקירה, פנסיה, השתלמות, חיסכון לכל ילד: 0 שגיאות עמוד, 0 גלילה אופקית.
- axe על הסקירה ועל פנסיה, מחשב וטלפון: 0 הפרות.
- תיקון במסירה: כותרת מסלול שמסתיימת בספרות ("תלוי גיל, עד 50") התמזגה עם המספר שאחריה. שני החלקים מבודדי־כיווניות עכשיו (`TrackCard.tsx`).

## מגבלות ידועות

- הטבלה המלאה רחבה מהמכל ב-1440 וגוללת אופקית בתוכו (מכוון; התצוגה המצומצמת מתאימה לרוחב).
- תחת `vite preview` אין את נתיבי `/api/*` והעמוד קורא ישירות מסופאבייס. בייצור הנתיב עובר דרך הקאש.
- הסיווג למסלולים מבוסס ביטויים בשם הקופה. קופות שלא זוהו נופלות ל"אחר" בכל מוצר.

## דרך חזרה

- אתר: `git revert` של קומיט המיזוג, או קידום הפריסה הקודמת ב-Vercel.
- נתונים: הלוח והוויו נוספו בלבד; שום טבלה קיימת לא שונתה. הסרה: `drop materialized view cma_board cascade` + החזרת `refresh-cma-funds-latest` לפקודה המקורית + `cron.unschedule('refresh-cma-board-monthly')`.
